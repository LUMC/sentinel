{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Unsupported Pipeline Summary",
    "description": "Schema for generic JSON files uploaded to Sentinel.",

    "anyOf": [
        { "type": "object", "minProperties": 1 },
        { "type": "array", "minItems": 1 }
    ]
}
